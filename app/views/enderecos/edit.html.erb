<%- model_class = Endereco -%>
<% if authCurrentUsuario() && ifCurrentEndereco() %>
<div class="page-header">
  <h1>
  	Editar endereço
  </h1>
</div>
<%= render :partial => 'form' %>
<% else %>
<p>Você não tem privilégios para esta página</p>
<% end %>